// `with` 式: scope-bound resource binding.
// 現状は let とほぼ同じ意味 (v0)。
// 将来的に scope を抜けるとリソース自動破棄 (Drop) と arena 紐付けが入る予定。

// 複数 binding を一度に: with x = ..., y = ..., z = ... in body
with one = 1,
     two = 2,
     three = 3
in
  one + two + three  // 6
